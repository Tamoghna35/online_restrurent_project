import { Router } from "express";
const router = Router();

// /tables: GET - View all tables and their layout.
router.route()
// / tables: POST - Add a new table or update the layout.
router.route()
// / tables /: tableId: PUT - Modify a specific table's details.
router.route()





export default router;